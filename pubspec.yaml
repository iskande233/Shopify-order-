name: hallaq_dz
description: "تطبيق حلاق ديزاد - النسخة الاحترافية"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Firebase Stable For FlutLab
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6

  # Location
  geolocator: ^10.1.0

  # URL Launcher
  url_launcher: ^6.2.2

  # مهم جدا لحل مشكلة PromiseJsImpl
  js: ^0.6.7
  web: ^0.1.4-beta

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true